% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_page_insights.R
\name{get_additional_page_info_db}
\alias{get_additional_page_info_db}
\title{Get Page Info Dataset for a Specific Country}
\usage{
get_additional_page_info_db(iso2c, verbose = TRUE)
}
\arguments{
\item{iso2c}{A string specifying the ISO-3166-1 alpha-2 country code (e.g., "DE", "FR", "US").}

\item{verbose}{Logical. If TRUE (default), prints a status message when downloading.}
}
\value{
A tibble containing Facebook page info for the specified country.
If the dataset is not available or cannot be retrieved, a tibble with \code{no_data = TRUE}
and the given \code{iso2c} code is returned.
}
\description{
Downloads the historical Facebook or Instagram page info dataset for a given ISO2C country code.
The data is retrieved from a fixed GitHub release URL in \code{.parquet} format. It includes information on:
\itemize{
\item Page-level metadata (e.g., name, verification status, profile type)
\item Audience metrics (e.g., number of likes, Instagram followers)
\item Shared disclaimers (if applicable)
\item Page creation and name change events with timestamps
\item Contact and address information (if available)
\item Free-text descriptions ("about" section)
}
}
\examples{
\dontrun{
  de_info <- get_page_info_db("DE")
  fr_info <- get_page_info_db("FR")
}

}
