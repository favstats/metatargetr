# Get the current browser session (internal)

Returns the active browser session, or NULL if none is active. This is
used internally by other functions.

## Usage

``` r
get_browser_session()
```

## Value

A chromote session object or NULL.
