# Extract fb_dtsg token from browser context (internal helper)

Extract fb_dtsg token from browser context (internal helper)

## Usage

``` r
.extract_fb_dtsg(b)
```

## Arguments

- b:

  A chromote session object.

## Value

Character string (the token) or NULL.
