# Parse rendered profile transparency text (internal)

Parse rendered profile transparency text (internal)

## Usage

``` r
.parse_profile_transparency_text(text)
```

## Arguments

- text:

  Character scalar (typically `document.body.innerText`).

## Value

List with `page_id` and `is_running_ads`.
