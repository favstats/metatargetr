# Convert an entry for an ad into a row in a tibble

We did not manage to easily output the entry for one ad as a row in a
tibble; this line solves the issue and does exactly that.

## Usage

``` r
stupid_conversion(x)
```

## Arguments

- x:

  An object containing data about the ad.

## Value

A tibble row.
